# Agent Instructions

## Repository Rules

- Always remove Windows Zone.Identifier files (e.g. `file.ext:Zone.Identifier`) from the repo.
- Never commit Zone.Identifier files; delete them immediately and keep them out of git.
- Keep `.gitignore` updated to exclude Zone.Identifier artifacts.
